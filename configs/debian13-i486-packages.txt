# Debian 13/i486 PC-98 rootfs package profile.
#
# Keep this list declarative: changing it invalidates the reusable rootfs
# cache and all three Debian release images are rebuilt from the new profile.

# Boot, devices, storage, and networking.
sysvinit-core
e2fsprogs
kmod
udev
ifupdown
iproute2
net-tools
dhcpcd-base
ca-certificates

# Core utilities, diagnostics, and interactive tools.
vim-tiny
vim
tmux
htop
procps
sysstat
less
curl
eject
wget
kexec-tools
iputils-ping
openssh-server
ucf
runit-helper

# Minimal X11 desktop used for PC-98 graphics validation.  The fbdev driver
# renders onto /dev/fb0, which the pc98tridentfb kernel module exposes for the
# on-board Trident TGUI96xx (or pc98cirrusfb on Cirrus-based models).
xorg
xserver-xorg-video-fbdev
xserver-xorg-input-evdev
xterm
twm
